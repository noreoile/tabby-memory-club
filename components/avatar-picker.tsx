"use client";
import {AVATAR_COUNT,avatarPhoto} from '@/lib/avatars';
import {RadioGroup,RadioGroupItem} from '@/components/ui/radio-group';
export function AvatarImage({id}:{id:number}){const photo=avatarPhoto(id);return <div className="cat" role="img" aria-label={photo.name+'頭像'} style={{backgroundImage:`url(${photo.image})`,backgroundPosition:`${photo.id%6*20}% ${Math.floor(photo.id/6)*20}%`}}/>}
export function AvatarPicker({value,onChange,disabled=false}:{value:string;onChange:(value:string)=>void;disabled?:boolean}){return <div className="avatar-options"><p>36 張貓咪頭像 · 狸花貓、橘貓、阿比西尼亞貓</p><div className="avatar-selected"><AvatarImage id={Number(value)}/><span>目前選擇</span></div><RadioGroup className="avatar-picker expanded" value={value} onValueChange={onChange} disabled={disabled} aria-label="選擇貓咪頭像">{Array.from({length:AVATAR_COUNT},(_,id)=>{const photo=avatarPhoto(id);return <RadioGroupItem value={String(id)} key={id} aria-label={`${photo.name}頭像 ${id%12+1}`} className={'cat-choice '+(value===String(id)?'selected':'')} style={{backgroundImage:`url(${photo.image})`,backgroundSize:'600% 600%',backgroundPosition:`${photo.id%6*20}% ${Math.floor(photo.id/6)*20}%`}}/>})}</RadioGroup></div>}
